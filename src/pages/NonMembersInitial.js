import styled from "styled-components";
import Layout from "../layouts/Layout";

export default function NonMembersInitial() {
  return <Layout headerLeft="logo"></Layout>;
}

const NonMembersInitialLayout = styled.div``;
